{
  "name": "docker-mount-demo",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "mocha ./test/*.test.js --timeout 10000"
  },
  "dependencies": {
    "selenium-webdriver": "^4.12.0",
    "chai": "^5.0.0"
  },
  "devDependencies": {
    "mocha": "^10.0.0"
  }
}
